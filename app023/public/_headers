# セキュリティヘッダー設定（Netlify / Cloudflare Pages用）
# このファイルは静的サイトのデプロイ時にセキュリティヘッダーを自動設定します

/*
  # Clickjacking対策
  X-Frame-Options: DENY

  # MIME sniffing対策
  X-Content-Type-Options: nosniff

  # XSS対策（レガシーブラウザ向け）
  X-XSS-Protection: 1; mode=block

  # Referrerポリシー
  Referrer-Policy: strict-origin-when-cross-origin

  # 不要な機能の制限
  Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()

  # Content Security Policy（CSP）
  # - script-src: Tone.js動作のため'unsafe-inline'を許可
  # - style-src: Tailwind CSSのため'unsafe-inline'を許可
  # - img-src: SVGアイコン・data URIサポート
  # - connect-src: Gemini API接続を許可
  # - frame-ancestors: iframe埋め込み禁止（Clickjacking対策）
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' https://generativelanguage.googleapis.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
